
import observer.JClockTimer;
import observer.JNumericTimer;
import subject.Controller;
import subject.PersonalTimer;

/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
/**
 *
 * @author Vincent Guidoux
 */
public class Labo1 {

    /**
     * @param args the command line arguments
     */
    public static void main(String[] args) {
        Controller controller = new Controller();
        /*JNumericTimer numTimer = new JNumericTimer();
        timer.attach(numTimer);
        JClockTimer arabicTimer = new JClockTimer("clock1.jpg");
        timer.attach(arabicTimer);
        JClockTimer romanTimer = new JClockTimer("clock2.jpg");
        timer.attach(romanTimer);*/
    }
}
